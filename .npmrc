# Packages must arrive over TLS. Pinned here as well as in any user config so
# a fork, a fresh machine or CI cannot quietly fall back to plaintext http,
# which also makes `npm audit` refuse to run and hides every JS advisory.
registry=https://registry.npmjs.org/

ignore-scripts=true
audit=true
